/**
 * OptoPupil - Display Light Stimulus & Screening Configuration
 * Centralized timing constants for the controlled visual stimulus and screening workflow.
 */

/**
 * Default stimulus duration in milliseconds.
 * Initial prototype baseline: 500 ms full-screen white stimulus.
 */
export const STIMULUS_DURATION_MS = 500;

/**
 * Required continuous stable bilateral pupil detection duration before triggering stimulus.
 * Default: 1000 ms (1.0 second).
 */
export const STABLE_DETECTION_MS = 1000;
